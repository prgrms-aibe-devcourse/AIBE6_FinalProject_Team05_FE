import type { Metadata } from "next";
export const metadata: Metadata = { title: "마켓 인사이트 - Pokade" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
