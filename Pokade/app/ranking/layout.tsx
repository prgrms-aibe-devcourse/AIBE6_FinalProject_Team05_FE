import type { Metadata } from "next";
export const metadata: Metadata = { title: "시세 랭킹 - Pokade" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
