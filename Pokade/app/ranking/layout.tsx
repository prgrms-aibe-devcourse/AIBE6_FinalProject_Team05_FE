import type { Metadata } from "next";
export const metadata: Metadata = { title: "시세 랭킹 - PocketTrade" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
